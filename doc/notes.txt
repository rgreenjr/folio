Bugs
====

- Navigation should check for inclusion of objects prior to deleting
- ePub validation: handle missing files and unregistered files
- NavigationController.deletePoints should handle points with children

Features
========

- Navigation: add points
- Verify all undo actions
- Delete cover image
- Unmark cover image

Enhancements
============

- Search filtering of selection views
- Ability to drag to manifest items to the desktop
- Add WebView forward/backward buttons
- Better error handling of ill-formed XML documents
- Drag and drop of directories into Manifest
- Perform background scan of XML for linkable elements
- Buttons to generate GUID
- Support "replace" when moving items in Manifest
- Show icon in spine indicating item is cover, toc, titlepage, etc.
- Add plus/minus buttons for adding Navigation points, Spine itemrefs, and manifest items
- Add icons to right of spine items in to show assigned type
- Add source code syntax highlighting

Refactoring
===========

- Use "box" in view placeholder names
- Move metadata, spine, navigation and manifest into container

Code Snippets
=============

def keyDown(event)
  key = event.characters.characterAtIndex(0)
  if (key == NSDeleteCharacter)
    # delete image here
  else #if (key == 27)
    super
  end
end

